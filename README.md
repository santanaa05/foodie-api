# foodie-api

## Como inciar o projeto

1. Instale as dependências:

   ```bash
   npm install
   ```

2. Inicie o app no ambiente web com o expo:

   ```bash
    npx expo start
   ```

## API utilizada
